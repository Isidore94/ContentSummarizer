# YouTube download + caption/audio extraction
yt-dlp

# Local GPU transcription fallback (RTX 3080 Ti / CUDA)
faster-whisper

# Summarization — OpenAI (default) and Anthropic (alternate via SUMMARY_PROVIDER)
openai
anthropic

# .env loading
python-dotenv

# GitHub REST API client (chosen over PyGithub for explicit, minimal calls)
requests
