# Runtime dependencies used to build the Windows mini-PC executable.
# Exact versions make rebuilds repeatable; update deliberately and retest.
yt-dlp==2026.7.4
openai==2.45.0
anthropic==0.116.0
python-dotenv==1.2.2
requests==2.34.2

# LAN web dashboard, hosted by the desktop app so other PCs can reach the UI.
fastapi==0.139.2
uvicorn==0.51.0
markdown==3.10.2
python-multipart==0.0.32
